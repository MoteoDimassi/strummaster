# StrumMaster v2.0 - Guitar Rhythm Trainer

StrumMaster - это комплексное веб-приложение для обучения игре на гитаре, включающее тренировку ритмических паттернов, музыкальный диктант, тюнер и другие учебные инструменты. Приложение позволяет воспроизводить, создавать и настраивать различные паттерны переборов, тренировать музыкальный слух через диктанты, а также включает встроенный тюнер для настройки гитары.

## Основные возможности

- 🎸 Создание и воспроизведение гитарных ритмических паттернов
- 🎵 Визуализация паттернов перебора с указанием направления (вверх/вниз)
- 🎼 Поддержка различных типов ударов (обычный, приглушенный, пропущенный)
- 🎛️ Встроенный тюнер с поддержкой хроматического и гитарного режимов
- 🔊 Различные стратегии воспроизведения (базовая, агрессивная, мягкая)
- 📝 Возможность добавления текста к каждому удару
- ⚙️ Настройка BPM (темпа) воспроизведения
- 🎹 **Мелодический диктант** - интерактивные упражнения на развитие слуха
- 🎼 Визуализация нот на нотном стане
- 🎹 Интерактивная пианино-клавиатура для ввода нот
- 📊 Система уровней сложности с настраиваемыми параметрами

## Технологический стек

- **Frontend**: React 19.2.0, TypeScript
- **State Management**: Redux Toolkit
- **Routing**: React Router DOM v7.10.1
- **Build Tool**: Vite
- **Audio**: Web Audio API
- **UI Components**: Lucide React Icons
- **Styling**: Tailwind CSS v4.1.17

## Архитектура приложения

Проект StrumMaster построен на основе модульной архитектуры с четким разделением ответственности, которая разделяет код на несколько ключевых уровней:

### Core-модули (`src/core/`)

Core-модули содержат основную бизнес-логику и не зависят от UI-фреймворков. Они инкапсулируют ключевую функциональность приложения:

- **AudioEngineCore** - ядро аудио-системы для воспроизведения звуков гитарных струн и аккордов
- **PlayerCore** - ядро проигрывателя паттернов с точным планировщиком времени
- **TunerCore** - ядро тюнера для анализа звуковых частот в реальном времени
- **DictationCore** - ядро музыкального диктанта, включающее:
  - **AudioClient** - клиент для синтеза и воспроизведения звуков
  - **musicTheory** - модуль музыкальной теории для генерации гамм и мелодий
  - **types** - типы данных для нот, гамм и настроек диктанта

### Domain-сущности (`src/domain/`)

Слой доменных сущностей определяет основные бизнес-модели приложения:

- **AudioConfig** - конфигурация аудио-системы (BPM, громкость)
- **Measure** - музыкальный такт с набором шагов перебора
- **StrumStep** - отдельный шаг перебора (направление, тип аккорда, текст)

### Features-модули (`src/features/`)

Features-модули содержат код, связанный с конкретными функциями приложения, включая UI-компоненты и сервисы, которые используют core-модули через адаптеры:

- **audio** - компоненты и сервисы для работы с аудио
- **player** - компоненты и сервисы проигрывателя паттернов
- **tuner** - компоненты и сервисы тюнера
- **dictation** - компоненты и сервисы музыкального диктанта:
  - **screens** - экраны настройки и тренировки диктанта
  - **components** - UI компоненты (пианино, нотный стан)

### Адаптеры (`src/core/adapters/`)

Адаптеры обеспечивают связь между core-модулями и внешними зависимостями (браузерными API, UI-фреймворками):

- **AudioEngineAdapter** - адаптер для AudioEngineCore
- **PlayerAdapter** - адаптер для PlayerCore
- **TunerAdapter** - адаптер для TunerCore
- **BrowserApiAdapter** - адаптеры для браузерных API

### Управление состоянием (`src/store/`)

Состояние приложения управляется через Redux Toolkit с разделением на срезы:

- **playerSlice** - состояние проигрывателя паттернов
- **tunerSlice** - состояние тюнера
- **dictationSlice** - состояние музыкального диктанта

## Структура проекта

```
strummaster/
├── samples/                    # Аудио образцы струн
│   ├── Mute.mp3               # Звук приглушенной струны
│   ├── 1e/, 2B/, 3G/         # Образцы для каждой струны
│   │   └── fret0-7.mp3       # Звуки для каждого лада
│   └── 4D/, 5A/, 6E/
├── src/
│   ├── core/                  # Core-модули с бизнес-логикой
│   │   ├── audio/            # Ядро аудио-системы
│   │   │   ├── AudioEngineCore.ts
│   │   │   └── index.ts
│   │   ├── player/           # Ядро проигрывателя
│   │   │   ├── PlayerCore.ts
│   │   │   └── index.ts
│   │   ├── tuner/            # Ядро тюнера
│   │   │   ├── TunerCore.ts
│   │   │   └── index.ts
│   │   ├── dictation/        # Ядро музыкального диктанта
│   │   │   ├── AudioClient.ts    # Клиент для синтеза звуков
│   │   │   ├── musicTheory.ts     # Музыкальная теория и генерация
│   │   │   ├── types.ts          # Типы данных для диктанта
│   │   │   └── index.ts
│   │   ├── adapters/         # Адаптеры для интеграции
│   │   │   ├── AudioEngineAdapter.ts
│   │   │   ├── PlayerAdapter.ts
│   │   │   ├── TunerAdapter.ts
│   │   │   ├── BrowserApiAdapter.ts
│   │   │   └── index.ts
│   │   └── index.ts          # Экспорт всех core-модулей
│   ├── domain/               # Сущности предметной области
│   │   └── entities/         # Интерфейсы AudioConfig, Measure, StrumStep
│   ├── features/             # Функциональные модули
│   │   ├── audio/            # Модуль аудио системы
│   │   │   └── services/
│   │   │       └── AudioEngineService.ts
│   │   ├── player/           # Модуль проигрывателя паттернов
│   │   │   ├── components/   # UI компоненты проигрывателя
│   │   │   └── services/
│   │   │       └── PlayerService.ts
│   │   ├── tuner/            # Модуль тюнера
│   │   │   ├── components/   # UI компоненты тюнера
│   │   │   └── services/
│   │   │       └── TunerService.ts
│   │   └── dictation/        # Модуль музыкального диктанта
│   │       ├── screens/      # Экраны диктанта
│   │       │   ├── SetupScreen.tsx    # Экран настроек диктанта
│   │       │   └── TrainingScreen.tsx # Экран тренировки
│   │       └── components/   # UI компоненты диктанта
│   │           ├── PianoKeyboard.tsx   # Интерактивная пианино-клавиатура
│   │           └── Stave.tsx          # Нотный стан для визуализации
│   ├── shared/               # Общие компоненты и утилиты
│   │   ├── components/       # Общие UI компоненты
│   │   └── utils/            # Утилиты (AudioEventBus, AudioSampleFactory)
│   ├── store/                # Управление состоянием Redux
│   │   └── slices/           # Redux срезы (player, audio, tuner)
│   ├── layouts/              # Компоновки страниц
│   │   └── MainLayout.tsx
│   ├── pages/                # Страницы приложения
│   │   ├── DictationPage.tsx # Страница диктанта
│   │   ├── TrainerPage.tsx   # Страница тренажера
│   │   ├── TunerPage.tsx     # Страница тюнера
│   │   └── ...               # Другие страницы
│   ├── App.tsx               # Основной компонент приложения
│   └── index.css             # Глобальные стили
├── index.html                # HTML точка входа
├── index.tsx                 # React точка входа
├── package.json              # Зависимости проекта
├── tsconfig.json             # Конфигурация TypeScript
└── vite.config.ts            # Конфигурация Vite
```

## Преимущества архитектуры

### 1. Переиспользуемость

Core-модули не зависят от UI-фреймворков и могут быть использованы в других проектах:

```typescript
// Пример использования AudioEngineCore в другом проекте
import { AudioEngineCore } from "strummaster/core";

const audioEngine = new AudioEngineCore();
await audioEngine.strum("down", "Am", "strum");
```

```typescript
// Пример использования DictationCore
import {
  AudioClient,
  generateScale,
  generateMelody,
} from "strummaster/core/dictation";

const audioClient = new AudioClient();
const scale = generateScale("C", "Major", [4], 5);
const melody = generateMelody(scale, 5, 3);
await audioClient.playSequence(
  melody.map((n) => ({ frequency: n.frequency, duration: 0.5 }))
);
```

### 2. Тестируемость

Разделение на core-модули и адаптеры упрощает модульное тестирование:

```typescript
// Пример тестирования PlayerCore с мок-адаптерами
const mockTimerAdapter = {
  setTimeout: jest.fn(),
  clearTimeout: jest.fn(),
};

const mockAudioEngineAdapter = {
  currentTime: 0,
  resume: jest.fn(),
  strum: jest.fn(),
  stopAllSounds: jest.fn(),
};

const playerCore = new PlayerCore(
  mockTimerAdapter,
  mockAudioEngineAdapter,
  mockEventBusAdapter
);
```

```typescript
// Пример тестирования DictationCore
import { AudioClient } from "strummaster/core/dictation";

const mockAudioContext = {
  createOscillator: jest.fn(),
  createGain: jest.fn(),
  currentTime: 0,
};

const audioClient = new AudioClient();
// Тестирование синтеза звуков и генерации мелодий
```

### 3. Модульность

Каждый core-модуль инкапсулирует свою логику и может быть разработан независимо:

- **AudioEngineCore** отвечает только за воспроизведение звуков гитары
- **PlayerCore** отвечает только за планирование и управление воспроизведением паттернов
- **TunerCore** отвечает только за анализ звуковых частот
- **DictationCore** отвечает только за музыкальный диктант и синтез звуков

## Модуль музыкального диктанта

Модуль диктанта позволяет пользователям развивать музыкальный слух через интерактивные упражнения:

### Основные компоненты

- **AudioClient** - синтезатор звуков для воспроизведения нот и мелодий
- **musicTheory** - модуль для генерации гамм и мелодий на основе музыкальной теории
- **SetupScreen** - интерфейс настройки параметров диктанта
- **TrainingScreen** - основной экран тренировки с пианино и нотным станом
- **PianoKeyboard** - интерактивная пианино-клавиатура для ввода нот
- **Stave** - нотный стан для визуализации мелодий

### Функциональность

- **5 уровней сложности** - от поступенного движения до скачков на октаву
- **Настраиваемые параметры**:
  - Тоника (любая нота от C до B)
  - Тип гаммы (мажор/минор)
  - Диапазон октав (Great, Small, 1st, 2nd)
  - Количество ступеней гаммы (3-7)
  - Длина мелодии (3-15 нот)
  - Лимит повторений мелодии
- **Два режима ввода**:
  - **Режим записи** - ввод нот без воспроизведения
  - **Пробный режим** - воспроизведение нот при нажатии
- **Визуальная обратная связь** - подсветка правильных и неправильных нот
- **Статистика** - счетчик успешных и ошибочных попыток

### Алгоритм генерации мелодий

1. Генерация гаммы на основе выбранной тоники и типа
2. Фильтрация ступеней гаммы согласно настройкам
3. Создание мелодии с учетом уровня сложности:
   - Уровень 1: максимальный интервал 2 полутона
   - Уровень 2: максимальный интервал 4 полутона
   - Уровень 3: максимальный интервал 7 полутонов
   - Уровень 4: максимальный интервал 9 полутонов
   - Уровень 5: максимальный интервал 12 полутонов

## Примеры использования core-модулей

### Использование AudioEngineCore

```typescript
import {
  AudioEngineCore,
  AudioContextAdapter,
  GainNodeAdapter,
} from "strummaster/core";

// Создание экземпляра AudioEngineCore
const audioEngine = new AudioEngineCore(
  audioContextAdapter, // Адаптер AudioContext
  gainNodeAdapter, // Адаптер GainNode
  sampleFactoryAdapter, // Адаптер фабрики сэмплов
  strategyFactoryAdapter, // Адаптер фабрики стратегий
  eventBusAdapter // Адаптер шины событий
);

// Воспроизведение перебора
await audioEngine.strum("down", "Am", "strum");

// Воспроизведение отдельной ноты
await audioEngine.playNote("1e", 3);

// Установка стратегии воспроизведения
audioEngine.setPlaybackStrategy("aggressive");
```

### Использование PlayerCore

```typescript
import {
  PlayerCore,
  TimerAdapter,
  AudioEngineAdapter,
  EventBusAdapter,
} from "strummaster/core";

// Создание экземпляра PlayerCore
const player = new PlayerCore(
  timerAdapter, // Адаптер таймера
  audioEngineAdapter, // Адаптер аудио движка
  eventBusAdapter // Адаптер шины событий
);

// Установка паттернов для воспроизведения
player.setMeasures(measures);

// Настройка конфигурации
player.setConfig({ bpm: 120, lookahead: 25.0, scheduleAheadTime: 0.1 });

// Начало воспроизведения
await player.start();

// Остановка воспроизведения
player.stop();
```

### Использование DictationCore

```typescript
import {
  AudioClient,
  generateScale,
  generateMelody,
  GameSettings,
} from "strummaster/core/dictation";

// Создание экземпляра AudioClient
const audioClient = new AudioClient();

// Генерация гаммы для диктанта
const scale = generateScale("C", "Major", [4], 5); // C-мажор, 1 октава, 5 ступеней

// Генерация мелодии для диктанта
const melody = generateMelody(scale, 5, 3); // 5 нот, уровень сложности 3

// Воспроизведение мелодии
const sequence = melody.map((note) => ({
  frequency: note.frequency,
  duration: 0.6,
}));
await audioClient.playSequence(sequence);

// Воспроизведение отдельной ноты
await audioClient.playTone(440, 0.5); // A4, 0.5 секунды

// Настройки диктанта
const settings: GameSettings = {
  rootNote: "C",
  scaleType: "Major",
  difficulty: 3,
  replayLimit: 5,
  scaleRange: 5,
  selectedOctaves: [4],
  melodyNoteCount: 5,
};
```

### Использование TunerCore

```typescript
import {
  TunerCore,
  AudioDataAdapter,
  AnimationAdapter,
} from "strummaster/core";

// Создание экземпляра TunerCore
const tuner = new TunerCore();

// Анализ частоты
const frequency = 440; // A4
const result = tuner.analyzeFrequency(frequency);

// Анализ частоты для гитарного строя
const guitarResult = tuner.analyzeFrequencyForGuitar(frequency);

// Получение ближайшей ноты гитарного строя
const closestNote = tuner.getClosestGuitarNote(frequency);

// Запуск непрерывного анализа
const stopAnalysis = tuner.startContinuousAnalysis(
  audioDataAdapter,
  animationAdapter,
  buffer,
  (result) => {
    if (result) {
      console.log(
        `Note: ${result.note}, Frequency: ${result.frequency}Hz, Cents: ${result.cents}`
      );
    }
  },
  () => isRunning
);
```

## Использование адаптеров для интеграции с UI

Адаптеры обеспечивают удобную интеграцию core-модулей с UI-кодом:

```typescript
// Использование AudioEngineAdapter в React-компоненте
import { audioEngineAdapter } from "strummaster/core/adapters";

function AudioPlayer() {
  const handlePlayStrum = async () => {
    await audioEngineAdapter.strum("down", "Am", "strum");
  };

  const handleVolumeChange = (volume: number) => {
    audioEngineAdapter.setVolume(volume);
  };

  return (
    <div>
      <button onClick={handlePlayStrum}>Play Strum</button>
      <input
        type="range"
        min="0"
        max="1"
        step="0.1"
        onChange={(e) => handleVolumeChange(parseFloat(e.target.value))}
      />
    </div>
  );
}
```

```typescript
// Использование PlayerAdapter в React-компоненте
import { playerAdapter } from "strummaster/core/adapters";

function PatternPlayer() {
  const [isPlaying, setIsPlaying] = useState(false);

  useEffect(() => {
    const unsubscribe = playerAdapter.onPlay(() => {
      setIsPlaying(true);
    });

    const unsubscribeStop = playerAdapter.onStop(() => {
      setIsPlaying(false);
    });

    return () => {
      unsubscribe();
      unsubscribeStop();
    };
  }, []);

  const handleStart = async () => {
    await playerAdapter.start();
  };

  const handleStop = () => {
    playerAdapter.stop();
  };

  return (
    <div>
      <button onClick={handleStart} disabled={isPlaying}>
        Start
      </button>
      <button onClick={handleStop} disabled={!isPlaying}>
        Stop
      </button>
    </div>
  );
}
```

```typescript
// Использование TunerAdapter в React-компоненте
import { tunerAdapter } from "strummaster/core/adapters";

function GuitarTuner() {
  const [tunerResult, setTunerResult] = useState(null);

  useEffect(() => {
    const startTuner = async () => {
      await tunerAdapter.start();

      tunerAdapter.startContinuousAnalysis((result) => {
        setTunerResult(result);
      });
    };

    startTuner();

    return () => {
      tunerAdapter.stop();
    };
  }, []);

  return (
    <div>
      {tunerResult && (
        <div>
          <p>Note: {tunerResult.note}</p>
          <p>Frequency: {tunerResult.frequency}Hz</p>
          <p>Cents: {tunerResult.cents}</p>
          <p>In Tune: {tunerResult.isInTune ? "Yes" : "No"}</p>
        </div>
      )}
    </div>
  );
}
```

## Навигация по приложению

Приложение использует React Router для навигации между различными модулями:

- **Главная страница** (`/`) - обзор всех доступных модулей
- **Тренажер паттернов** (`/trainer`) - создание и воспроизведение гитарных паттернов
- **Мелодический диктант** (`/dictation`) - тренировка музыкального слуха
- **Тюнер** (`/tuner`) - настройка гитары с визуальной обратной связью
- **Метроном** (`/metronome`) - инструмент для отработки ритма
- **Табулатуры** (`/tabs`) - просмотр и создание табулатур
- **Блог** (`/blog`) - статьи и уроки по игре на гитаре
- **Курсы** (`/courses`) - структурированные обучающие курсы

## Запуск приложения

**Предварительные требования:** Node.js

1. Установка зависимостей:

   ```bash
   npm install
   ```

2. Запуск приложения в режиме разработки:

   ```bash
   npm run dev
   ```

3. Сборка приложения для продакшена:

   ```bash
   npm run build
   ```

4. Предпросмотр собранного приложения:
   ```bash
   npm run preview
   ```

## Особенности реализации

- Использование Web Audio API для высокоточного воспроизведения звуков гитарных струн
- Синтез звуков для музыкального диктанта с использованием осцилляторов Web Audio API
- Ленивая загрузка аудио образцов для оптимизации производительности
- Поддержка различных стратегий воспроизведения для создания разнообразных звуков
- Реактивное обновление UI через события аудио системы
- Адаптивный дизайн для работы на различных устройствах
- Модульная архитектура с разделением на core-модули и UI-слой
- Адаптеры для интеграции core-модулей с внешними зависимостями
- Алгоритмическая генерация мелодий на основе музыкальной теории
- Визуализация нот на нотном стане с поддержкой добавочных линий
- Интерактивная пианино-клавиатура с подсветкой нот гаммы
- Система уровней сложности с настраиваемыми параметрами
